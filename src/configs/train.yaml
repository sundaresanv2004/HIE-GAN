batch_size: 2
num_workers: 4

optimizer:
  type: "adam"
  lr: 1e-4
  weight_decay: 0.0

scheduler:
  use_scheduler: false

epochs: 50
log_every: 50
save_every: 1

loss:
  chamfer_weight: 1.0
  sdf_weight: 0.0
  gan_weight: 0.0

device: "cuda"
checkpoint_dir: "outputs/checkpoints"
