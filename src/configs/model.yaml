device: "cuda"     # automatic fallback to CPU if no GPU

feature_extractor:
  type: "resnet18"
  out_dim: 256

explicit_branch:
  init_mesh:
    template: "sphere"
    subdivisions: 2       # ~642 vertices
  gcn_hidden_dims: [ 128, 64 ]
